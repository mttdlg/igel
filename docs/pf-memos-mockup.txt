fun f { int x ; int y ; int z } int {
     
}

form while-counter { e exp ; b body ; max_iter [exp int] } block {
    var cnt-id [gensym] # Counter var name
    later { do {
        var [now.cnt-id] [now.max_iter]
        while ( [now.e] and ( [now.cnt-id] > 0 ) ) {
            do [now.b]
            dec [now.cnt-id] }}}}
vars {
    a b
    c d
    e f
} {
    cond {
        c    r
        c    r
        c    r
        true r
    }
}
